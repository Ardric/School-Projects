#include "Dijkstra.h"
#include <queue>
using namespace std;


int dijkstra (const Graph<string>& graph, string start, string finish)
{
  priority_queue< ??? > agenda;
  agenda.push( start ??? );

  while (not agenda.empty())
  {
  }

  // not found
  return -1;
}
